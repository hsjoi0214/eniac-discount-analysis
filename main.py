from data_loader import create_valid_path, products_cl, orderlines_cl, orders_cl, brands_cl, df
from data_processing import *
from visualization import *

# The script was originally written to run top to bottom, so
# ensure any required function calls or plotting calls are placed here.
# You can add: plot_top_categories_by_revenue(df)  # for example